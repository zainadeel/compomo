{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "badge",
  "title": "Badge",
  "description": "Compact count indicator with selection state.",
  "type": "registry:ui",
  "meta": {
    "library": "@compomo/ui",
    "distribution": "npm",
    "storybook": "https://zainadeel.github.io/CompoMo/?path=/story/badge",
    "consumption": {
      "install": "npm install @compomo/ui @ds-mo/tokens",
      "cssSetup": "Import tokens CSS at your app entry point: import '@ds-mo/tokens/css';\nThis provides all var(--color-*), var(--dimension-*), var(--effect-*), and var(--typography-*) custom properties.\nFor dark mode, set data-theme=\"dark\" on <html>.",
      "import": "import { Badge } from '@compomo/ui';",
      "cssImport": "import '@compomo/ui/css';",
      "typeImport": "import type { BadgeProps } from '@compomo/ui';",
      "internalDependencies": [
        {
          "component": "Text",
          "note": "Used internally by Badge. No action needed — it's bundled in @compomo/ui."
        }
      ],
      "peerDependencies": {
        "required": [
          "react >=17",
          "react-dom >=17",
          "@ds-mo/tokens >=0.1.0"
        ],
        "optional": []
      }
    },
    "props": {
      "count": {
        "type": "number",
        "required": true,
        "description": "Returns null if 0. Shows '+' if > 9."
      },
      "isSelected": {
        "type": "boolean",
        "default": "false"
      }
    },
    "exports": [
      "Badge"
    ],
    "types": [
      "BadgeProps"
    ]
  },
  "dependencies": [
    "@compomo/ui",
    "@ds-mo/tokens"
  ],
  "registryDependencies": [
    "text"
  ],
  "files": [
    {
      "path": "src/components/Badge/Badge.module.css",
      "content": ".badge {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  pointer-events: none;\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "src/components/Badge/Badge.tsx",
      "content": "import React from 'react';\nimport { Text } from '@/components/Text';\nimport styles from './Badge.module.css';\n\nexport interface BadgeProps {\n  count: number;\n  isSelected?: boolean;\n  className?: string;\n  /**\n   * Accessible label announced by screen readers. Takes precedence over `labelFormat`.\n   * Provide this when the badge has a specific meaning (e.g. `\"3 unread messages\"`).\n   */\n  'aria-label'?: string;\n  /**\n   * Formats the accessible label from the count. Used when `aria-label` is not provided.\n   * Defaults to returning the count as a string.\n   */\n  labelFormat?: (count: number) => string;\n}\n\nexport const Badge: React.FC<BadgeProps> = ({\n  count,\n  isSelected = false,\n  className,\n  'aria-label': ariaLabel,\n  labelFormat,\n}) => {\n  if (count === 0) return null;\n  const displayText = count > 9 ? '+' : count.toString();\n  const accessibleLabel = ariaLabel ?? labelFormat?.(count) ?? String(count);\n  return (\n    <span className={`${styles.badge} ${className || ''}`} aria-label={accessibleLabel}>\n      <Text variant=\"text-caption-emphasis\" as=\"span\" color={isSelected ? 'primary' : 'secondary'}>\n        {displayText}\n      </Text>\n    </span>\n  );\n};\n\nBadge.displayName = 'Badge';\n",
      "type": "registry:ui"
    }
  ]
}