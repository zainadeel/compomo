# Published Code Connect templates

Add only **`*.figma.ts`** files here that point at **real** Figma nodes (`// url=` from *Copy link to selection*). The CLI validates each URL against the file before publish.

Copy from `../DsIcon.figma.ts.example` to start, then fix the `// url=` line. Files in this folder are included by `figma.config.json`.
